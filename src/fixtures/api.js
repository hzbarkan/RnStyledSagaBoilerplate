/**
 * Keep all API endpoints here
 * Currently filled with mocks, replace later
 */
const AUTH_URL = '/auth'
export const LOGIN_URL = `${AUTH_URL}/login`
export const LOGOUT_URL = `${AUTH_URL}/logout`
export const REFRESH_TOKEN_URL = `${AUTH_URL}/refresh`
